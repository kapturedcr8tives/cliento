apiVersion: apps/v1
kind: Deployment
metadata:
  name: crm-system
  namespace: crm-staging
---
apiVersion: v1
kind: Service
metadata:
  name: crm-service
  namespace: crm-staging
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: crm-ingress
  namespace: crm-staging
  annotations:
    kubernetes.io/ingress.class: "nginx"
    cert-manager.io/cluster-issuer: "letsencrypt-staging"
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
spec:
  tls:
  - hosts:
    - staging.crm.yourdomain.com
    secretName: crm-tls
  rules:
  - host: staging.crm.yourdomain.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: crm-service
            port:
              number: 80
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: crm-hpa
  namespace: crm-staging